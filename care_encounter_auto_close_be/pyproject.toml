[project]
name = "care_encounter_auto_close_be"
version = "0.1.0"
description = "This is a care backend plugin enabling encounter to auto complete with respect to time , encounter class and status"
readme = "README.md"
authors = [
  {name = "Open Health Care Network", email = "support@ohc.network"}
]
maintainers = [
  {name = "Open Health Care Network", email = "support@ohc.network"}
]
classifiers = [
    # TODO
]
license = {text = "MIT"}
dependencies = [
    "django",
    "celery",
    "djangorestframework",
]
requires-python = ">= 3.10"

[project.optional-dependencies]
test = [
    "ruff",  # linting
]

[project.urls]
bugs = "https://github.com/ohcnetwork/care_encounter_auto_close_be/issues"
changelog = "https://github.com/ohcnetwork/care_encounter_auto_close_be/blob/master/changelog.md"
homepage = "https://github.com/ohcnetwork/care_encounter_auto_close_be"


[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.uv]
package = true
